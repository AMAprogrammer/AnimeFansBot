from pyrogram import Client
from local_BotConfig import *

api_id = api_ID
api_hash = api_HASH
bot_token = token

bot = Client(
    "AnimeBot",
    api_id=api_id,
    api_hash=api_hash,
    bot_token=bot_token
    )